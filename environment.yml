name: configistate
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.8
  - pip
  - setuptools
  - wheel
  - toml
  - tomli>=2.0  # For Python < 3.11
  - click>=8.0.0
  - pytest>=7.0.0
  - black>=22.0.0
  - flake8>=4.0.0
  - isort>=5.10.0
  - pip:
    - -e .